import type { ConversationSummary } from "@/lib/services/conversation-admin-service";
import { dateTimeFormatter } from "@/components/shared/formatters";

export function ConversationList({ conversations, selectedPsid, onSelect }: { conversations: ConversationSummary[]; selectedPsid: string; onSelect: (psid: string) => void }) {
  return <aside className="min-w-0 max-w-full overflow-hidden border-b border-zinc-200 bg-white lg:border-b-0 lg:border-r" aria-label="Danh sách hội thoại"><div className="flex items-center justify-between px-4 pb-3 pt-4"><div className="min-w-0"><h2 className="font-black">Hội thoại</h2><p className="truncate text-xs text-zinc-400">{conversations.length} cuộc trò chuyện · Vuốt để xem</p></div><span className="size-2 shrink-0 rounded-full bg-emerald-500" aria-label="Hệ thống trực tuyến" /></div><div className="flex w-full max-w-full snap-x snap-mandatory gap-2 overflow-x-auto overscroll-x-contain px-3 pb-4 lg:block lg:max-h-[calc(100vh-12rem)] lg:space-y-1 lg:overflow-y-auto">
    {conversations.map((conversation) => <button key={conversation.psid} type="button" aria-pressed={selectedPsid === conversation.psid} onClick={() => onSelect(conversation.psid)} className={`min-w-[calc(100vw-4.5rem)] max-w-72 snap-center rounded-xl p-3 text-left transition-colors duration-200 focus-visible:ring-2 focus-visible:ring-red-600 min-[480px]:min-w-72 lg:w-full lg:min-w-0 lg:max-w-none ${selectedPsid === conversation.psid ? "bg-red-50 ring-1 ring-red-100" : "bg-zinc-50 hover:bg-zinc-100 lg:bg-transparent"}`}><div className="flex items-start justify-between gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-full bg-zinc-900 text-xs font-black text-white" aria-hidden="true">{conversation.customerName.split(" ").at(-1)?.[0] ?? "K"}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{conversation.customerName}</p><p className="mt-0.5 truncate text-xs text-zinc-500">{conversation.lastMessage || "(chưa có tin nhắn)"}</p></div><time className="shrink-0 text-[10px] font-semibold text-zinc-400" dateTime={conversation.updatedAt}>{dateTimeFormatter.format(new Date(conversation.updatedAt))}</time></div><div className="mt-2 flex flex-wrap items-center justify-between gap-1"><span className={`rounded-full px-2 py-0.5 text-[10px] font-bold ${conversation.mode === "human" ? "bg-amber-100 text-amber-800" : "bg-blue-50 text-blue-700"}`}>{conversation.mode === "human" ? "Nhân viên hỗ trợ" : "Trợ lý AI"}</span>{conversation.storeName ? <span className="rounded-full bg-red-50 px-2 py-0.5 text-[10px] font-bold text-red-700">{conversation.storeName}</span> : null}{conversation.activeOrderId ? <span className="font-mono text-[10px] font-bold text-zinc-400" translate="no">{conversation.activeOrderId}</span> : null}</div></button>)}
  </div></aside>;
}
