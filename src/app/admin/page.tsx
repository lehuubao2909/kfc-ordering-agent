/**
 * Admin dashboard. OWNER: Dev C.
 * TODO(Dev C): 4 cards — đơn live (polling 2s) · funnel started→cart→confirmed→paid→delivered ·
 * upsell acceptance rate · NLU eval "18/20" (đọc từ scripts/eval-results.json). Ghi rõ assumptions.
 */
export default function AdminDashboardPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-bold">Admin Dashboard</h1>
      <p className="text-gray-500">TODO(Dev C): live orders + funnel + upsell + NLU eval cards</p>
    </main>
  );
}
