/**
 * Staff console. OWNER: Dev C.
 * TODO(Dev C): 2 panel — (1) hội thoại: list + transcript polling 2s + nút Tiếp quản/Trả lại bot + ô gõ tay;
 * (2) đơn hàng: bảng orders + nút advance status (gọi /api/admin/orders/advance).
 */
export default function StaffConsolePage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-bold">Staff Console</h1>
      <p className="text-gray-500">TODO(Dev C): conversations + takeover + order status advance</p>
    </main>
  );
}
