/**
 * Seed menu từ src/fixtures/menu-sample.json vào Neon. OWNER: Dev A.
 * Chạy: npm run script scripts/seed-menu.ts
 * TODO(Dev A): mở rộng fixtures lên ~50-80 món thật từ website KFC VN (kèm ảnh) rồi chạy lại.
 */
// TODO(Dev A): đọc fixtures → validate MenuItemSchema → upsert vào menuItems + seed vouchers/promotions mẫu
console.log("TODO(Dev A): seed-menu");
